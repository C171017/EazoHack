# 决策登记

## 已确认与变更

| 编号 | 状态 | 结论 |
| --- | --- | --- |
| D01 | 已确认，替代旧提案 | 阅读 → 高亮 → 路由到一种或多种辅助 → 探索 → 保存到段落/地图；不再以图谱选概念作为求助起点 |
| D05 | 输入已确认；触发细节待决 | MVP 以左侧原书选中/高亮文字作为上下文。截图与周期采集在 MVP 外；节点/当前页不能替代选文 |
| D06 | 产品范围已确认；实现深度待决 | 路由器可组合实时生成交互 UI、生成图片、SVG/概念图/思维导图、来源查找四路；撤销此前 SVG-only 默认范围 |

2026-09-05：用户明确授权更新六份文档，未授权实现。四路意图与高亮输入是确认结果；此前关于这两项的开放选择已失效。以下讨论不得重新降级它们为未确定愿景。

## 后续讨论顺序

| 顺序 / 编号 | 待决问题 | 建议及取舍 |
| --- | --- | --- |
| 1 / D06 | 十小时每路深度，尤其交互 UI 生成方式？ | 四路保留并演示组合；交互 UI 先聚焦一个经验证的窄主题，由 LLM 实时生成配置/内容与组件组合；自由代码生成需要隔离和验证。具体深度尚未批准 |
| 2 / D09 | 来源查找覆盖书内还是外部？ | 演示暂提议书内检索相关段落；若批准外部检索，再选服务、核验来源及处理失败。不能把“书内提案”写成已定范围 |
| 3 / D05 | 高亮后何时启动、如何组合/改选？ | 建议高亮后明确点击求助，显示路线理由并允许改选；规则、并发、数量、成本控制待定 |
| 4 / D02 | 示例书、节点和边语义？ | 短知识类书，概念节点、章节分组；小说/论证型书会改变图 schema |
| 5 / D03 | 首发文件和整书规模？ | 有文字层、排版简单的 PDF；TXT 有余量再加；不接受静默截断 |
| 6 / D04 | 2D 或 3D？ | 暂建议 2D 稳定布局，把时间留给高亮和四路辅助 |
| 7 / D07 | 活动指标、空间书签与映射？ | 主动事件计数，避免段落到多概念重复计量；不表示理解程度 |
| 8 / D08 | 基础栈、模型/图片/检索服务及环境？ | Next.js、PDF.js、React Flow、IndexedDB 暂定；四路 provider 与生成实现需预检后选择 |

每次确认追加：`日期 | D编号 | 状态 | 用户结论 | 影响文档`。当前没有选定具体模型、价格、外部检索范围或应用实现方案。
