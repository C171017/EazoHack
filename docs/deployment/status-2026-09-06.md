# Domain and Vercel update — 2026-09-06

- Vercel CLI authorization succeeded; all 16 configured application variables were installed as sensitive values in Preview only. Analysis remains disabled until hosted federation checks are complete.
- `read.vin` is attached to `c171017/eazo-hack`. Spaceship DNS now has apex A records `216.198.79.1` and `64.29.17.1`, TTL 30 minutes. Vercel verifies the configuration and ownership. Nameservers remain Spaceship. No Cloudflare or other domain configuration changed.
- Fresh release snapshot `/private/tmp/eazo-release-domain` passed `VERCEL=1 npm run build:vercel`, including asset/tracing checks.
- Both the default and explicit Preview CLI uploads were classified as Production by Vercel. Both were removed. Do not repeat these uploads. Vercel staff confirm first-deployment Production behavior; an inert no-alias bootstrap was proposed but automatic approval review rejected it pending explicit Production bootstrap authorization. No bootstrap was created.
- The app is not yet serving at read.vin. The application needs a true Preview deployment, domain alias, hosted authentication/federation verification, and final functional checks.

# Current deployment status — 2026-09-06

This section supersedes the historical audit below.

- Supabase `eazo-hack-demo` (`gzmomicvppjckgzbbimm`), Free/Tokyo: migrations 1–5 applied through SQL Editor. Reconcile CLI migration history before any `db push`; do not reapply them. Private sources, graph publication, fenced checkpoints, quotas and duplicate dispatch reservations are installed.
- Cloud Run `eazo-book-analysis`, project `eazo-hack-20260905-c1710`, region `asia-east1`: deployed with one task, 1 CPU, 2 GiB, 7200-second timeout and two platform retries. Worker identity has Vertex and its dedicated secret access. Invoker permission is confined to this job.
- Image: `asia-east1-docker.pkg.dev/eazo-hack-20260905-c1710/eazo-workers/book-analysis@sha256:c42bdb039030f793491961ab3632ab788359f1802eb8f5b5e03ee8f8f24b0b4b`; pipeline `worker-dd8ea048e3a052ac1249`.
- WIF `eazo-vercel/eazo-preview` trusts only `owner:c171017:project:eazo-hack:environment:preview`. It can impersonate the job invoker and existing Gemini assistance identity. Production is excluded.
- The Supabase server credential is in Secret Manager version 1 and a permission-restricted temporary local test configuration. It has NOT yet been installed in Vercel.
- Vercel project `c171017/eazo-hack` (`prj_2WvzNk9MP5PY6Qti5qVZF6DVQLUz`) exists with Node 22, Next.js and protected Previews. No Preview deployment yet; CLI device authorization remains pending. No public Production release.

## Verified

- 195 tests and lint passed. Isolated release build passed at `/private/tmp/eazo-release-final`; 11 HTTP smoke checks passed on the earlier isolated release. Later concurrent UI edits require a fresh release snapshot before upload.
- Local SQL assertions passed for owner isolation, leases, retry limits, checkpoints, quotas and duplicate dispatch protection.
- Real Supabase/application test: two synthetic accounts log in; one cannot list, open or download the other's source. Normalized text retains the original-file identity and loads in the reader. Logout prevents anonymous library access.
- One approved small AI test succeeded on attempt 1: job `ee936b13-358d-458c-ba2f-592dadede84e`, Cloud Run execution `eazo-book-analysis-qqhqw`. It published one graph version; the authenticated app validated source, manifest, graph and hierarchy and loaded the result. The other account could not read its graph version.
- Two synthetic test accounts and one synthetic book/result remain for Preview validation. No real book was submitted to this test. The US$10 authorization covers initial deployment and this small test; exact billing has not been reconciled.

## Remaining

Complete Vercel CLI sign-in, set Preview-only environment values, deploy the latest validated source and verify Vercel identity federation plus the full hosted reader/map/assistance flow. Hosted analysis remains disabled in the temporary web configuration until that wiring is ready. Original PDFs remain local; cloud saving currently copies extracted text. Accounts are invitation-only; public signup/SMTP and general release are not configured. Shanghai mobile/Wi-Fi verification remains pending. Interrupted jobs have explicit resume, not an automatic scheduler.

## Historical audit (superseded)

# Deployment audit — 2026-09-06

The three platform tasks prepared local packages; they did not complete deployment.
This audit rechecked the current checkout and browser accounts. No cloud resources,
repository permissions, secrets, billing settings, or deployments were changed.

## Completed in this audit

- Integrated the Vercel preparation patch into current main (e21e595), preserving
  all four pre-existing reader/map/style edits. Nothing was committed or pushed.
- Prepared an isolated release-check copy in `/private/tmp/eazo-deployment-review-20260906`
  so the running development server and its build directory remain separate.
- Latest source: 183 tests passed; lint passed; whitespace validation passed.
- Confirmed Vercel session under c171017. Opened the GitHub App installation page
  and selected ONLY C171017/EazoHack. Installation has NOT been submitted.
- Opened Supabase sign-in in Chrome. No authenticated Supabase organization or
  project is available for inspection yet.
- Google Cloud browser is signed in. The project's linked-billing page redirects
  to an existing paid billing account with a payment method. No payment change is needed
  based on this inspection. The local gcloud CLI has no signed-in account.

## Actual integration gap (development work, not user setup)

Do not apply both platform SQL packages as if they implement the same contract.

| Concern | Supabase package (4991) | Worker package (32a2) |
|---|---|---|
| Job table | analysis_jobs | eazo_analysis_jobs |
| Buckets | eazo-sources / eazo-analysis | analysis-inputs / analysis-private |
| Lease | 5 minutes; claim/heartbeat/complete RPCs | 120 seconds; eazo_worker action RPC |
| Object limit | 50 MiB | 16 MiB |
| Result publication | graph_versions and published object paths | checkpoint index and final-result pointer |

The worker's SQL is a reference contract, not a deployable companion migration
for the Supabase schema. Reconcile worker storage, checkpoint persistence, source IDs,
lease renewal, attempt limits and atomic publication against one canonical schema,
then rerun SQL access-control and worker integration tests before any remote migration.

Still missing from the application: account/session UI and callbacks, protected
AI endpoints with quotas, explicit local-library export and cloud saving, authenticated
job submission/status/reconciliation, and loading completed private graph results.
Setting environment variables alone does not implement these features. These are
agent-owned implementation tasks; do not ask the user to write or debug them.

## User checkpoints

1. GitHub: approve/install the Vercel app for ONLY C171017/EazoHack. The app asks
   for read/write code, administration, checks, commit statuses, deployments, issues,
   pull requests, hooks and workflows, plus read actions/metadata. The browser policy
   requires confirmation at this access grant; the Install button remains untouched.
2. Supabase: sign in to the intended account. Account creation/terms and choosing
   a paid organization cannot be inferred from Vercel login. Keep credentials out of chat.
3. After account inspection, identify the exact free organization/project/region,
   final reconciled migrations and SMTP choice before provisioning. SMTP is necessary
   for general email-confirmed signup; team-only testing can precede that decision.
4. Cloud deployment requires the exact service-account/IAM trust and a test-spending
   limit to be reviewed once the integration is ready. Existing billing is not permission
   for unbounded paid model executions. No need to add a new card now.

## Release gates

- Protected Vercel preview of the reviewed latest source; no automatic public main import.
- Provider environment values entered securely, with production/preview separation.
- Supabase real Auth/Storage two-user isolation checks, and one budget-approved Cloud Run job.
- Interrupted/retried processing must resume checkpoints without stale publication.
- Shanghai Wi-Fi/mobile tests and full reader/upload/map/AI smoke checks.
- A separate concrete public-release decision after the full stack passes.

The historical platform documents record earlier authorization boundaries and test
results. They are background evidence, not new user instructions or proof of deployment.

## Latest validation qualification

The fresh isolated production build failed initially because Google Fonts could not
be fetched inside the sandbox. A network-enabled IPv4-first retry stalled after TLS
errors and was deliberately stopped. Therefore the earlier worktree's successful
build is NOT a successful build of the latest source. No new production HTTP smoke
or browser test is claimed. This remaining build/network check is agent-owned work.

The Google Cloud Jobs dashboard was inspected: its jobs table is empty and the page
states that creating the first job will enable the Cloud Run Admin API. No job/API
creation was submitted.

## Continued after user completed GitHub installation and Supabase sign-in

- Created empty Vercel project `c171017/eazo-hack`, ID
  `prj_2WvzNk9MP5PY6Qti5qVZF6DVQLUz`; no deployment was triggered.
- Saved Node 22.x and Next.js framework. Existing Standard Protection requires
  Vercel login for previews. Build/installation commands remain in vercel.json.
- Supabase organization: C171017's Org, Free, `brawtsvrsoydhbwsynmk`.
  Existing project `bwwvldtkzjogddlixspi` belongs to C171017/Network (us-west-2)
  and was not modified.
- Prepared a separate eazo-hack-demo creation form, specific Tokyo region,
  Data API on, automatic exposure of new tables off, automatic RLS on.
  Password blank; user must enter/save the credential and submit creation.
- Copied the Supabase migrations and local SQL tests into the current checkout.
  No remote migration applied; worker contract reconciliation still required.

## Supabase provisioned and baseline applied

User created `gzmomicvppjckgzbbimm` (eazo-hack-demo). Dashboard confirms Healthy,
Free organization, Tokyo. Public project URL:
https://gzmomicvppjckgzbbimm.supabase.co

Read-only preflight returned zero public tables. Applied the SQL equivalent of
202609060001_eazo_private_library.sql and 202609060002_analysis_leases.sql via
Dashboard SQL Editor, each transaction returned Success. These were manual SQL
applications, not CLI migrations; reconcile migration history before future db push.
No user records, book files or model calls were created.

Six hosted catalog assertions passed: six tables have RLS, both buckets private,
anonymous users cannot read books, authenticated users cannot insert jobs or call
claim_analysis_job, service_role can claim jobs. This is catalog validation, not
a complete two-user Auth/Storage HTTP end-to-end test.

Started gcloud browser authorization. The local CLI waits for its verification code
in execution session 86844. Chrome Google Cloud SDK page remains open. Automatic
approval review rejected clicking Continue because broad cloud-platform, compute,
App Engine, SQL login and offline OAuth access requires explicit current approval.
User must approve that page; do not bypass the rejection or request codes in chat.
The deployment/app integration, worker contract reconciliation, fresh build, and
protected preview remain incomplete. Supabase URL is known; secrets have not been
copied to the app or Vercel. No paid execution has run.

## Google SDK continuation

User completed the Google consent page. Attempted CLI verification returned
`invalid_grant`; the login process exited. This is not a successful CLI login.
Do not reuse the old code or record credential values. Browser control then failed
three times (inventory/direct-tab requests timed out), preventing a fresh OAuth
flow. Asked user to reconnect the Chrome extension. The exact reason Google
rejected the code is unconfirmed.

Started a fresh isolated build with a 150-second process-group timeout. Shared
checkout has additional concurrent reader/assistance edits; no edits were overwritten
and no source was committed/pushed. No further cloud resources changed this turn.
The isolated build reached its 150-second limit (124) during optimized compilation
and was stopped. Its log showed no successful compilation or new specific error;
the latest source is therefore still not build-verified.

## CLI retry succeeded

Chrome extension continued timing out, but native Chrome app control worked.
The earlier code transcription had a case-sensitive character error; this was
confirmed from the native page text. Restarted the flow and used the fresh native
page result. Google CLI login succeeded for c171017howie@gmail.com, project
`eazo-hack-20260905-c1710`, numeric ID `592771495287`, ACTIVE.
Read-only inventory confirms Vertex AI enabled and existing service account
`eazo-hack-gemini@eazo-hack-20260905-c1710.iam.gserviceaccount.com` enabled.
Cloud Run, Artifact Registry, Secret Manager and IAM APIs were not in the enabled
service list. No resources were created and no billable model request executed.
